name: Windows v518 Account Dropout Survival

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  account-dropout-v518:
    name: Python ${{ matrix.python }} x64 — v518 only
    runs-on: windows-2022
    timeout-minutes: 35

    strategy:
      fail-fast: false
      matrix:
        python: ["3.13", "3.14"]

    env:
      PYTHONUTF8: "1"
      PYTHONIOENCODING: "utf-8"
      QT_QPA_PLATFORM: "offscreen"

    steps:
      - name: Checkout exact audit ref
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python ${{ matrix.python }} x64
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python }}
          architecture: "x64"
          cache: "pip"

      - name: Install hash-locked test environment
        shell: pwsh
        run: |
          python -m ensurepip --upgrade
          python -m pip install --require-hashes -r requirements-bootstrap.txt
          python -m pip install --require-hashes --no-build-isolation -r requirements-runtime.lock
          python -m pip install --require-hashes --no-build-isolation -r requirements-openai.lock
          python -m pip install --require-hashes -r requirements-dev-windows-x64.lock
          python -m pip check

      - name: Compile v518 only
        shell: pwsh
        run: |
          python -m py_compile tests/test_v518_account_dropout_survival.py

      - name: Run v518 account-dropout scenarios only
        shell: pwsh
        run: |
          python -m pytest `
            tests/test_v518_account_dropout_survival.py `
            -vv -s `
            -o addopts="" `
            --tb=long `
            --showlocals `
            --maxfail=0 `
            --junitxml=v518-results-${{ matrix.python }}.xml `
            2>&1 | Tee-Object -FilePath v518-${{ matrix.python }}.log

          exit $LASTEXITCODE

      - name: Upload v518 diagnostics
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: v518-account-dropout-python-${{ matrix.python }}-${{ github.sha }}
          path: |
            v518-${{ matrix.python }}.log
            v518-results-${{ matrix.python }}.xml
          if-no-files-found: error
